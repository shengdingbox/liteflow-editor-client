import { NODE_TYPE_END } from '../../constant';
import icon from './icon.svg';

const config = {
  label: '结束',
  type: NODE_TYPE_END,
  icon,
  node: {
    primer: 'circle',
  },
};

export default config;
